# personal-dashboard Worker (Commodores / Ops / plays / morning page).
# Cloudflare Git runs `npx wrangler versions upload` from repo root.
# Without this file the Workers Build fails with "Missing entry-point".
# Static assets only — no Worker script. Do not add a `main` field.
name = "personal-dashboard"
compatibility_date = "2026-08-27"
workers_dev = true

[assets]
directory = "."
html_handling = "auto-trailing-slash"
not_found_handling = "none"
